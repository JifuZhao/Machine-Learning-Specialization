package week1

object exercise {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(40); val res$0 = 
  1 + 2;System.out.println("""res0: Int(3) = """ + $show(res$0))}
}
